<div id="aihelper-chat-conversation">
  <% @conversation.messages.each do |message| %>
    <div class="message">
      <strong><%= message.role == 'user' ? @user : massage.role %></strong>
      <pre><%= message.content %></pre>
    </div>
  <% end %>
</div>